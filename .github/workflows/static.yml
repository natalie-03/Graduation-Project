name: Dcard Crawler Daily

on:
  workflow_dispatch:
  schedule:
    - cron: '0 16 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 方案 B：明確指定使用 .github/workflows 裡的 requirements.txt
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

          REQUIRE_FILE=".github/workflows/requirements.txt"

          if [ -f "$REQUIRE_FILE" ]; then
            echo "📦 Installing dependencies from $REQUIRE_FILE"
            pip install -r "$REQUIRE_FILE"
          else
            echo "⚠️ 找不到 requirements.txt，改用預設套件"
            pip install requests pandas cloudscraper
          fi

      - name: Run Crawler
        run: python .github/workflows/scraper.py
        timeout-minutes: 15

      - name: Commit and Push Data
        if: always()
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"

          mkdir -p csv

          if ls csv/*.csv 1> /dev/null 2>&1; then
            git add csv/*.csv
            git commit -m "Auto-update data [skip ci]" || echo "⚠️ 沒有變更"
            git push
          else
            echo "💤 無 CSV 產生，跳過推送"
          fi
