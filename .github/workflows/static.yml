name: Dcard Crawler Daily

on:
  workflow_dispatch:
  schedule:
    - cron: '0 16 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Setup Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          
          # 如果專案根目錄有 requirements.txt 優先用它
          if [ -f requirements.txt ]; then
            echo "📦 Installing from requirements.txt"
            pip install -r requirements.txt
          else
            echo "⚠️ 找不到 requirements.txt，改用預設套件"
            pip install requests pandas cloudscraper
          fi

      # Step 4: Run crawler python
      - name: Run Crawler
        run: python scraper.py
        timeout-minutes: 15

      # Step 5: Commit and Push Data
      - name: Commit and Push Data
        if: always() 
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          
          mkdir -p csv

          # 如果有 csv 才提交
          if ls csv/*.csv 1> /dev/null 2>&1; then
            echo "✅ 發現 CSV 檔
