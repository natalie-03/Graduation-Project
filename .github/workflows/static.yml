name: Dcard Crawler
on:
  schedule:
    - cron: '0 2 * * *'  # 每天凌晨2點執行
  workflow_dispatch:  # 手動觸發

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        
    - name: Run crawler
      run: python dcard_crawler.py
      timeout-minutes: 15
      
    - name: Upload results
      uses: actions/upload-artifact@v4  # 更新到 v4 版本
      with:
        name: dcard-data
        path: csv/
